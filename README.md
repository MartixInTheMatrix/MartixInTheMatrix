# Hi there 👋

```ts
class Me extends Life.Boy<Father> {
  name: string;
  age: number;
  languages: string[];

  constructor(){
    name = 'Martix';
    age = 17;
    langages = ['JS/TS', 'C#', 'Java', 'C', 'Python'];
  }
}
  
```
